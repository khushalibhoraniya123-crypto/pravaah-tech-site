{
  "name": "pravaah_technology",
  "version": "1.0.0",
  "description": "Pravaah Technology - Modern Full-Stack Technology Agency Website",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": ["react", "vite", "typescript", "express", "mongodb", "technology-agency", "pravaah-technology"],
  "author": "Pravaah Technology",
  "license": "ISC"
}
